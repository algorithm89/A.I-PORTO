# Production overrides ? DB and Vault are on the same server (localhost)
spring.cloud.vault.uri=http://127.0.0.1:8200
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/bublikstd?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
# This is the URL users click in confirmation emails ? must be publicly reachable
# Change to your domain when ready (e.g. https://api.bublikstudios.net)
app.base-url=http://152.53.210.206:8080

# Turn off noisy debug logs in production
logging.level.org.springframework.cloud.vault=WARN
logging.level.org.springframework.vault=WARN
logging.level.org.springframework.cloud.vault.config=WARN
logging.level.com.zaxxer.hikari=INFO
logging.level.org.hibernate.SQL=WARN
spring.jpa.show-sql=false

